# Leeg = relatieve `/api/...` (gebruik met Vite proxy naar Next op :3000).
# Productie-Vite op ander domein: volledige origin van de Next-app (zonder slash).
# VITE_GENTRIX_API_BASE=https://www.gentrix.nl
#
# Bij cross-origin: op de Next-server (Vercel) zetten:
# BOOKING_VITE_PUBLIC_ORIGINS=https://jouw-booking-app.netlify.app
# (komma’s voor meerdere origins)
