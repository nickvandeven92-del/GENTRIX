# Memory: index.md
Updated: now

# Project Memory

## Core
Kameleon webshop module — theme-agnostic engine using CSS variables/design tokens.
All webshop components use semantic tokens only — no hardcoded colors.
Dutch language for UI labels (NL).
Multi-language NOT needed — user explicitly skipped.

## Memories
- [Webshop architecture](mem://features/webshop-architecture) — Module structure, components, integration patterns
